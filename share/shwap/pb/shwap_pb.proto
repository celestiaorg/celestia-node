syntax = "proto3";

import "types.proto";

message RowResponse {
    bytes row_id = 1;
    Row row = 2;
}

message SampleResponse {
    bytes sample_id = 1;
    ShareWithProof sample = 2;
}

message DataResponse {
    bytes data_id = 1;
    Data data = 2;
}
