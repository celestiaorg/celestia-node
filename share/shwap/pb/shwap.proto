syntax = "proto3";

import "types.proto";

message RowBlock {
    bytes row_id = 1;
    HalfAxis row = 2;
}

message SampleBlock {
    bytes sample_id = 1;
    ShareWithProof sample = 2;
}

message DataBlock {
    bytes data_id = 1;
    NamespacedRow data = 2;
}
